# C Word Frequency Analyzer (Hash Table Implementation)

Ένα εργαλείο ανάλυσης κειμένου σε γλώσσα **C**, το οποίο υπολογίζει τη συχνότητα εμφάνισης κάθε λέξης χρησιμοποιώντας μια δομή **Hash Table** για μέγιστη ταχύτητα.

## Τεχνικά Χαρακτηριστικά
* **Hash Table με Chaining**: Χρήση συνδεδεμένων λιστών (linked lists) για τη διαχείριση συγκρούσεων (hash collisions).
* **djb2 Hash Function**: Υλοποίηση του αλγορίθμου του Dan Bernstein για αποτελεσματικό hashing συμβολοσειρών.
* **Text Pre-processing**: Αυτόματη μετατροπή σε πεζά γράμματα και καθαρισμός συμβόλων/στίξης από τις λέξεις.
* **Memory Management**: Πλήρης διαχείριση δυναμικής μνήμης με καθαρισμό όλων των κόμβων για την αποφυγή memory leaks.

## Πώς Λειτουργεί
1. Το πρόγραμμα δέχεται ένα κείμενο εισόδου.
2. Διαχωρίζει το κείμενο σε λέξεις (tokenization).
3. Κάθε λέξη περνάει από τη συνάρτηση κατακερματισμού (hash function) και εισάγεται στον πίνακα.
4. Αν η λέξη υπάρχει ήδη, αυξάνεται ο μετρητής της.
5. Στο τέλος, εκτυπώνονται τα αποτελέσματα και απελευθερώνεται η μνήμη.

## Τεχνολογίες
* **C (C99/C11)**
* **Standard Libraries**: `stdio.h`, `stdlib.h`, `string.h`, `ctype.h`

## Οδηγίες Μεταγλώττισης
Μπορείτε να το τρέξετε σε οποιονδήποτε C compiler:
`gcc main.c -o word_analyzer`
`./word_analyzer`
